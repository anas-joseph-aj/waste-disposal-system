services:
  mongodb:
    image: mongo:7
    container_name: wds-mongodb
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: wds_ai_chatbot
    volumes:
      - wds_mongo_data:/data/db

volumes:
  wds_mongo_data:
